"""Repository-test fixtures.

Step 10 moved the temporary SQLite lifecycle to tests/conftest.py so
Repository and later Service/E2E tests share one canonical fixture.
Fixtures defined at tests/ scope are inherited automatically here.
"""
